{
  "build": {
    "builder": "dockerfile",
    "context": "."
  },
  "deploy": {
    "startCommand": "uvicorn curiosity_intelligence.api.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health"
  }
}
