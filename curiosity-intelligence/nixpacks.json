{
  "providers": ["python"],
  "python": {
    "version": "3.11"
  },
  "buildCommand": "pip install -r requirements.txt",
  "startCommand": "uvicorn curiosity_intelligence.api.main:app --host 0.0.0.0 --port $PORT --workers 1"
}
